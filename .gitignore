# 의존성/빌드 산출물 (GitHub Actions가 빌드 시점에 생성 — dev-plan.md 7장 참고)
node_modules/
dist/
dist-ssr/
*.tsbuildinfo

# 로그
*.log
npm-debug.log*

# 에디터/OS
.DS_Store
.vscode/*
!.vscode/extensions.json
*.local

# 환경변수 (배포 자격증명은 GitHub Actions Secrets로 관리 — 커밋 금지)
.env
.env.*
